# Vite reads this file automatically during `npm run build`.
# Vercel will use this value unless VITE_API_URL is also set
# in the Vercel dashboard (dashboard value takes precedence).
#
# Replace the URL below with your actual Render backend URL.
VITE_API_URL=https://mwamba-pos-api.onrender.com
